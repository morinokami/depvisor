{
  "name": "depvisor",
  "version": "1.5.0",
  "private": true,
  "description": "GitHub dependency-update PR aftercare: analyze, repair, verify, and report.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.ts",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run fmt:check && pnpm run knip && pnpm test",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "knip": "knip && knip --strict",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flue/runtime": "1.0.0-beta.9",
    "valibot": "1.4.2",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@flue/cli": "1.0.0-beta.9",
    "@types/node": "24.13.2",
    "knip": "6.24.0",
    "oxfmt": "0.57.0",
    "oxlint": "1.72.0",
    "oxlint-tsgolint": "0.24.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.11.0"
}
