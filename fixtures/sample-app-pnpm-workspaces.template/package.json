{
  "name": "sample-pnpm-workspaces",
  "version": "1.0.0",
  "private": true,
  "description": "Throwaway pnpm-workspaces target for depvisor prototyping. Deps are intentionally outdated; semver is catalog-pinned.",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  }
}
