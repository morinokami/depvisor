{
  "name": "sample-bun-workspaces",
  "version": "1.0.0",
  "private": true,
  "description": "Throwaway bun-workspaces target for depvisor prototyping. Deps are intentionally outdated.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter='*' build",
    "test": "bun run --filter='*' test"
  }
}
