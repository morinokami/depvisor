{
  "name": "sample-workspaces",
  "version": "1.0.0",
  "private": true,
  "description": "Throwaway npm-workspaces target for depvisor prototyping. Deps are intentionally outdated.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
